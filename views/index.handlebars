<nav class="navbar navbar-expand-lg navbar-light bg-light" id="nav">
  <a class="navbar-brand" href="https://github.com/Limbach17/burger">
    <img src="/assets/img/Burger-PNG-HD-Free-Vector-1024x1024.png" width="30" height="30" alt="lil-burger">
  </a>  
  <a class="navbar-brand" href="#">Burger Jawn</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    
    <form class="form-inline" id="create-form">
        <input class="form-control mr-sm-2" type="input" placeholder="create" aria-label="create">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Add a burger to the menu...</button>
    </form>
  </div>
</nav>

{{!-- <img src="/assets/img/Burger-banner.png" class="img-fluid" alt="Burger Banner" id="burger-banner"> --}}

<div class="container-fluid">
    
    <div class="row">
        <div class="col-lg-6">
            <h3>Burgers awaiting consumption</h3>
            <ul class="list-group">
                {{#each burgers}}
                    {{#unless devoured}}
                    {{> burgers/burger-block devoured=true}}
                    {{/unless}}
                {{/each}}
                
            </ul>
        </div>

        <div class="col-lg-6">
            <h3>Burgers downed</h3>
            <ul class="list-group">
                {{#each burgers}}
                    {{#if devoured}}
                    {{> burgers/burger-block devoured=false}}
                    {{/if}}
                {{/each}}
            </ul>

        </div>
    </div>
</div>



